# Result Schema

The initial scaffold returns a minimal discovery result:

- `status`
- `source`
- `summary`
- `signals`
- `workflow`
- `interpretation`
- `metadata`

The schema will be formalized before the first stable release.
